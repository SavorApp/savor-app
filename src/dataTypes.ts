// File for defining non-core data types
// used throughout the application.

// Please keep core data types within redux/ files

// Set InputUser type
// InputUser is received from the user at loginScreen
export type InputUser = {
    username: string,
    password: string
}